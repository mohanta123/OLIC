import 'package:flutter/material.dart';

class AppColors {
  // 🌞 PRIMARY – Energy / Sun (Logo Orange)
  static const secondary = Color(0xFFF2994A); // ORANGE
  static const primary = Color(0xFF27AE60);

  // 🌿 SUCCESS – Crop / Normal
  static const success = Color(0xFF27AE60); // Green

  // 🌊 INFO – Water / IoT
  static const info = Color(0xFF0681AB); // Blue-Teal

  // 🔴 DANGER – Errors / High usage
  static const danger = Color(0xFFE25C4A); // Red

  // 🧱 BACKGROUNDS
  static const background = Color(0xFFF9FAFB);
  static const card = Colors.white;

  // 📝 TEXT
  static const textPrimary = Color(0xFF1F2937);
  static const textSecondary = Color(0xFF6B7280);
  static const textLight = Color(0xFF9CA3AF);

  // 🧩 BORDERS / DIVIDER
  static const border = Color(0xFFE5E7EB);
  static const divider = Color(0xFFE5E7EB);


  // EXACT UI COLORS (DO NOT CHANGE)
  static const water = Color(0xFF7479CD);       // Purple-blue
  static const electricity = Color(0xFFE25C4A); // Red-orange
  static const leakage = Color(0xFF3C9F4F);     // Green
  static const alert = Color(0xFF0681AB);       // Blue
}
